import React from 'react';
import Register from '../components/auth/Register'; // fixed path

const RegisterPage = () => {
  return (
    <main className="flex-grow-1 py-4 container">
      <Register />
    </main>
  );
};

export default RegisterPage;
